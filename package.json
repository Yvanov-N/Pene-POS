{
  "name": "pene-pos",
  "private": true,
  "version": "0.0.0",
  "description": "Cite Shop / Pene POS - offline-first Point of Sale PWA monorepo",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
