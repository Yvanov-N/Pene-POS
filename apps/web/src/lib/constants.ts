// Stable, language-independent sentinel for "no category filter applied" --
// kept separate from its translated display label (t('pos.filters.allCategory'))
// so switching language never breaks an active filter comparison.
export const ALL_CATEGORIES_VALUE = "__all__";
